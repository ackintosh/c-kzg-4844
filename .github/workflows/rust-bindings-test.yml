name: Rust bindings tests
on:
  push:

jobs:
  #test-rust-bindings:
  #  runs-on: ubuntu-latest
  #  steps:
  #    - uses: actions/checkout@v3
  #      with:
  #        submodules: recursive
  #    - name: Get latest version of stable rust
  #      run: |
  #        rustup update stable
  #    - name: Build and Test (mainnet preset)
  #      run: |
  #        cd bindings/rust
  #        cargo clean
  #        cargo test -vv --all --release --tests
  #    - name: Build and Test (minimal preset)
  #      run: |
  #        cd bindings/rust
  #        cargo clean
  #        cargo test -vv --all --release --features="minimal-spec" --tests
  test-rust-bindings-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - uses: ilammy/msvc-dev-cmd@v1
      - name: Get latest version of stable rust
        run: |
          rustup update stable
      - name: test
        run: |
          #cd src
          #make blst

          # *** bat ***
          cd blst
          ./build.bat
          ls ./

          #pwd
          #ls ./
          #ls ../blst

          # *** build c_kzg_4844 ***
          cd ../src
          make test-ack
          ls ./
          lib /out:ckzg.lib c_kzg_4844.o
          ls ./

          # ******
          #cd ../src
          #cl /DFIELD_ELEMENTS_PER_BLOB=4096 /c /O2 /I../blst/bindings c_kzg_4844.c ../blst/blst.lib
          #cl /DFIELD_ELEMENTS_PER_BLOB=4096 /LD /I../blst/bindings c_kzg_4844.c ../blst/blst.lib
          
          # *** make all ***
          #cd ../src
          #make all FIELD_ELEMENTS_PER_BLOB=4096
          #ls ./

      - name: Build and Test (mainnet preset)
        run: |
          cd bindings/rust
          cargo clean
          cargo test --all --release --tests
          #- name: Build and Test (minimal preset)
          #  run: |
          #    cd bindings/rust
          #    cargo clean
          #    cargo test -vv --all --release --features="minimal-spec" --tests
